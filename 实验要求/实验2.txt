1、枚举类型CPU_Rank表示cpu的型号，枚举类型CPU_Rank的元素为i3，i5，i7。修改枚举元素的初始值，令i5=3

要求：（1）定义CPU_Rank类型的变量；
          （2）为变量初始化（值可以是i3，i5，i7中的任意一个），并输出该变量值；（可以直接通过cout输出）
          （3）新定义一个枚举变量，输入一个整数（值为任意枚举元素所对应的整数），将其赋给该枚举变量，输出变量值；
          （4）尝试直接为枚举元素赋值；尝试直接为变量通过键盘输入值。


2、 主函数中定义一个字符串变量str="original"。（通过string类来创建变量str，参考教材例6-23）

    定义两个函数change1和change2，分别以按值传递和按引用传递的方式接收字符串string，两个函数都将形参字符串的内容改为"alter"

    在主函数中首先调用change1函数，将string传给change1函数，输出string的内容

    然后调用change2函数，将string传给change2函数，再次输出string的内容

    观察两次string内容的不同，理解按值传递和按引用传递两种机制