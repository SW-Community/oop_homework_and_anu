实验要求：

1. 添加cpu类的构造函数：无参（输出“正在默认构造对象”）和带参数的（用于生成cpu对象时提供初始值，并输出“正在构造对象”）。带参数的使用初始化列表的形式
2. 添加cpu类的复制构造函数（输出“正在复制构造对象”）
3. 添加cpu类的析构函数（输出“正在删除对象”）
4. 在主程序中以不提供初始值和提供初始值的形式定义两个cpu对象
5. 生成第三个cpu，仿照p112，实现调用复制构造函数的三种情况






 