实验要求：


 1. 动态创建一个cpu的数组，数组中有两个cpu对象元素。
 
 2. 仿照例6-18（p222）创建cpu的动态数组类ArrayOfCpu

 3.将动态数组类ArrayOfCpu的复制构造函数改写为深复制
   
   在主函数中：
   
   分别设置两个cpu对象的参数，输出参数值，最后删除整个对象数组。

   创建动态数组类ArrayOfCpu的对象cpuArray1，设置对象中动态数组的两个对象元素cpu1和cpu2的值；

   创建动态数组类ArrayOfCpu的对象cpuArray2，将cpuArray1复制给cpuArray2；

   输出cpuArray2数组中两个cpu对象的值；

   重新设置cpuArray1数组中两个cpu对象的值，数值与第一次设置的值不同；   

   再次输出cpuArray2数组中两个cpu对象的值。




